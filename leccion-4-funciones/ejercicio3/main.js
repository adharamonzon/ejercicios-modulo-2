'use strict';
//Crea una función que reciba por parámetro un número, que representará un precio, y devuelva un texto en el que ponga el precio sin IVA, el IVA (21%) y el total. Por ejemplo,
// si pasamos por parámetro un 10, la función pintará en la consola "Precio sin IVA: 10, IVA: 2,1 y Total: 12,1".

/* function iva(x) {
  result`precio sin iva ${x}, IVA: x`;
}
console.log(finalPrice(price + priceIva)); */

//FUNCIÓN ARROW:
const iva = x => {
  result`precio sin iva ${x}, IVA: x`;
};
