'strict use';

const name = document.querySelector('p');
const compiName = 'Eva lopez merino';

name.innerHTML = `el nombre de mi compañera es ${compiName} y está compuesto por ${compiName.length}`;
